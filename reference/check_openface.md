# Check that openface is accessible

Check that openface is installed and accessible and working properly.

## Usage

``` r
check_openface()
```

## Value

A logical indicating whether openface is working (TRUE) or not (FALSE).

## Examples

``` r
if (FALSE) { # \dontrun{
check_openface()
} # }
```
