# Check that opensmile is accessible

Check that opensmile is installed and accessible and working properly.

## Usage

``` r
check_opensmile()
```

## Value

A logical indicating whether opensmile is working (TRUE) or not (FALSE).

## Examples

``` r
if (FALSE) { # \dontrun{
check_opensmile()
} # }
```
