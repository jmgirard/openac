# List openSMILE configuration files

Return a list of all configuration (.config) files found in the
openSMILE installation folder.

## Usage

``` r
os_list_configs()
```

## Value

A character vector containing the configuration files found.

## Examples

``` r
if (FALSE) { # \dontrun{
os_list_configs()
} # }
```
