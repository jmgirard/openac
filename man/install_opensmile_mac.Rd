% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/programs_install.R
\name{install_opensmile_mac}
\alias{install_opensmile_mac}
\title{Install openSMILE on macOS}
\usage{
install_opensmile_mac(
  download_url = NULL,
  install_dir = NULL,
  arch = c("armv8", "x86_64")
)
}
\arguments{
\item{download_url}{An optional string giving the URL of the openSMILE macOS
archive to download. If \code{NULL}, a pinned official release matching \code{arch}
is used.}

\item{install_dir}{An optional string giving the directory to install into.
If \code{NULL}, a per-user data directory (via \code{\link[rappdirs:user_data_dir]{rappdirs::user_data_dir()}}) is
used.}

\item{arch}{The CPU architecture to install for, either \code{"armv8"} (Apple
silicon) or \code{"x86_64"} (Intel). Ignored when \code{download_url} is supplied.}
}
\value{
A logical: \code{TRUE} on success, \code{FALSE} if the download or directory
creation failed.
}
\description{
Download a prebuilt openSMILE release for macOS and install it into a local
directory, then register the \code{SMILExtract} location with openac.
}
\examples{
\dontrun{
install_opensmile_mac()
}
}
