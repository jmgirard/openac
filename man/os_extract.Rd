% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_opensmile.R
\name{os_extract}
\alias{os_extract}
\title{Extract opensmile features}
\usage{
os_extract(
  infile,
  wavfile = NULL,
  aggfile = NULL,
  lldfile = NULL,
  config = "misc/emo_large",
  audio_args = list()
)
}
\arguments{
\item{infile}{(character) What is the filepath for the input file to be
analyzed? The proper format can be created by \code{os_prep_audio()}.}

\item{wavfile}{(character, default=NULL) Either NULL or a string indicating
the path to save the prepared version of \code{infile} to (must end with '.wav').
If NULL, a temporary file will be created and later discarded.}

\item{aggfile}{(character, default=NULL) What is the filepath to write the
AGG output to? If \code{NULL}, the AGG output will not be saved. Note that either
\code{aggfile} or \code{lldfile} (or both) must be non-NULL.}

\item{lldfile}{(character, default=NULL) What is the filepath to write the
LLD output to? If \code{NULL}, the LLD output will not be saved. Note that either
\code{aggfile} or \code{lldfile} (or both) must be non-NULL.}

\item{config}{(character, default="misc/emo_large") Which configuration file
should be used to analyze \code{infile}? A list of available config files can be
generated using \code{os_list_configs()}.}

\item{audio_args}{A list of optional arguments to forward to
\code{\link{os_prep_audio}}.}
}
\value{
A character vector including opensmile output.
}
\description{
Extract openSMILE acoustic features from an audio file based on a config
file. Lower level descriptors (LLDs) will be calculated per frame and then
summarized into an aggregate (AGG) file.
}
