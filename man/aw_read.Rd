% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_whisper.R
\name{aw_read}
\alias{aw_read}
\title{Read a Whisper transcription into a tidy tibble}
\usage{
aw_read(x)
}
\arguments{
\item{x}{A Whisper transcription: the object returned by \code{\link[=aw_transcribe]{aw_transcribe()}},
or a string path to its \code{.rds} or \code{.csv} output.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per segment and columns
\code{segment} (integer), \code{from}/\code{to} (numeric seconds), \code{text} (character),
and --- for diarized transcriptions --- \code{speaker} (character).
}
\description{
Turn the result of \code{\link[=aw_transcribe]{aw_transcribe()}} into a tidy \link[tibble:tibble]{tibble}
with one row per transcript segment. Accepts the transcription object
itself, or a string path to the \code{.rds} (full object) or \code{.csv} (\verb{$data})
file that \code{\link[=aw_transcribe]{aw_transcribe()}} writes --- all three forms yield identical
output.
}
\details{
Whisper's \code{HH:MM:SS.mmm} start/end timestamps are parsed to numeric seconds.
Segment text is preserved verbatim (Whisper often emits a leading space). A
\code{speaker} column is kept when (and only when) the transcription carries one
--- i.e. it was produced with diarization. The redundant \code{segment_offset}
(the start time re-encoded in milliseconds) is dropped, as is token-level
output (out of scope).
}
\examples{
\dontrun{
res <- aw_transcribe("audio.wav", model = aw_get_model("tiny"))
segments <- aw_read(res)
}
}
\seealso{
\code{\link[=aw_transcribe]{aw_transcribe()}}, which produces the input.
}
