% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_opensmile.R
\name{os_read}
\alias{os_read}
\title{Read openSMILE output into a tidy tibble}
\usage{
os_read(file)
}
\arguments{
\item{file}{(character) Path to an openSMILE output CSV, as written by
\code{\link[=os_extract]{os_extract()}} (its \code{aggfile} or \code{lldfile}).}
}
\value{
A \link[tibble:tibble]{tibble} with one row per observation and one
column per openSMILE metadata field and feature.
}
\description{
Read an openSMILE output CSV --- either an aggregate/functionals file
(from \code{-csvoutput}, one row) or a low-level descriptor file (from
\code{-lldcsvoutput}, one row per frame) --- into a wide
\link[tibble:tibble]{tibble}. There is one row per observation and one column
per feature, alongside the openSMILE metadata columns (\code{name}, and
\code{frameTime} for LLD output).
}
\details{
The delimiter is detected automatically, so both the native
semicolon-delimited output openSMILE writes and the comma-delimited form
produced by \code{\link[=os_fix_csv]{os_fix_csv()}} are accepted. Feature names are preserved
verbatim, including non-syntactic names such as \code{pcm_fftMag_mfcc[1]}.
}
\examples{
\dontrun{
os_extract("audio.wav", aggfile = "agg.csv", lldfile = "lld.csv")
agg <- os_read("agg.csv")
lld <- os_read("lld.csv")
}
}
\seealso{
\code{\link[=os_extract]{os_extract()}}, which produces the output files.
}
