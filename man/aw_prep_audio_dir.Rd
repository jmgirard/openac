% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_whisper.R
\name{aw_prep_audio_dir}
\alias{aw_prep_audio_dir}
\title{Run aw_prep_audio() on multiple files in a directory}
\usage{
aw_prep_audio_dir(
  indir,
  inext,
  outdir,
  recursive = FALSE,
  progress = c("auto", "on", "off"),
  ...
)
}
\arguments{
\item{indir}{(string) What directory contains the input files?}

\item{inext}{(string) What file extension should be looked for in \code{indir}
(e.g., "mp4" or "mp3")? Matched regardless of case, so "mp4" also takes
\code{.MP4} files; if that leaves two inputs deriving the same output file, the
batch is refused rather than one silently overwriting the other.}

\item{outdir}{(string) What directory should the audio files be output to?}

\item{recursive}{(logical, default = FALSE) Should files in subdirectories
within \code{indir} be included?}

\item{progress}{(string, default = "auto") Controls progress reporting.
One of \code{"auto"}, \code{"on"}, or \code{"off"}.
\itemize{
\item \code{"auto"}: Emit \code{progressr} signals but do not force display; a progress
bar appears only if the caller has enabled a handler (e.g.,
\code{progressr::with_progress()} or a global handler).
\item \code{"on"}: Wraps the call in \code{progressr::with_progress()} so progress will
render using any available handler.
\item \code{"off"}: Suppresses progress signals via \code{progressr::without_progress()}.
}}

\item{...}{
  Arguments passed on to \code{\link{aw_prep_audio}}
  \describe{
    \item{\code{stream}}{An optional number indicating the index of the audio stream in
\code{infile} to convert or extract. Note that ffmpeg uses zero-indexing so the
default of 0 is the first stream. Defaults to 0.}
    \item{\code{overwrite}}{Should outfile be overwritten if it already exists? It will
be skipped otherwise --- silently for a direct call, and as a \code{"skipped"}
row when run under one of the \verb{*_dir()} batch wrappers. Defaults to TRUE.}
    \item{\code{afilters}}{Should audio filters be used to try to improve audio quality?
(See Details.) Defaults to FALSE.}
  }}
}
\value{
(Invisibly) a data frame with one row per input file, giving the
\code{infile} and \code{outfile} it was called with, its #'   \code{status}, whether it \code{success}ed, and the \code{error} message if it did not.
\code{status} is one of \code{"ok"} (the operation completed), \code{"skipped"} (the file
was deliberately not processed) or \code{"failed"} (the operation raised an error);
\code{success} is \code{status == "ok"}, so a skipped file reads \code{FALSE}, and
\code{error} carries the reason for a skipped file as well as for a failed one.
A file that fails is skipped with a warning rather than aborting the
batch.
}
\description{
Find all media files with a specified extension in a specified directory and
then extract an audio file for acoustic analysis from each.
}
\details{
Can be optionally run in parallel by running \code{\link[future]{plan}()}
beforehand, e.g., by calling \code{plan("multisession", workers = 4)}.
}
