# References index

_One line per ingested source: `citekey` — title — traces to (tests/milestones)._
_PDFs live in `pdf/` (gitignored); summaries are `<citekey>.md` (committed)._

- `audiowhisper` — audio.whisper `predict.whisper()` result structure — M03 (`aw_read`)
